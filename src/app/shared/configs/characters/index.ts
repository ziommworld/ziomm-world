import { CharacterLibrary } from "src/app/$character";

import { neo } from "./player/neo.config";
import { beetle } from "./npc/beetle.config";


export const characters: CharacterLibrary = {
  neo,
  beetle,
};
